Algoritmo "Exercicio 3"

//Verifica o que será impresso na tela ao serem executados

Var
// Seção de Declarações das variáveis 
a, b, c: inteiro

Inicio
// Seção de Comandos, procedimento, funções, operadores, etc... 
a <- 10
b <- 20
c <- a//c passa a ter o mesmo valor que a
b <- c//b passa a ter o mesmo valor que c
a <- b//a passa a ter o mesmo valor que b
escreval(a, b, c)
Fimalgoritmo